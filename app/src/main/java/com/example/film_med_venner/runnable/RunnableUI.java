package com.example.film_med_venner.runnable;

public interface RunnableUI {
    void  addRunnableMovieUIOnFailure(RunnableMovieUI runnableMovieUI);

    void addRunnableMovieUIOnSuccess(RunnableMovieUI runnableMovieUI);

}
