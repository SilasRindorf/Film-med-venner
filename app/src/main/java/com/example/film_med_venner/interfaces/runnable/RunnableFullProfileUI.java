package com.example.film_med_venner.interfaces.runnable;

import com.example.film_med_venner.DTO.FullProfileDTO;

public interface RunnableFullProfileUI {
    void run(FullProfileDTO fullProfileDTO);
}
